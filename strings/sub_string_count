#include<stdio.h>
void main(){
    char a[50];
    char b[10];
    printf("Enter the string:");
    scanf("%s",a);
    printf ("\nEnter the substring:");
    scanf("%s",b);
    int i=0,j=0,k,count=0;
    while(a[i]!='\0'){
        if(a[i]!=b[j]){
        i++;
    }
    else{
        k=i+1;
        while(b[j]!='\0'){
            if(a[i]!=b[j])
            break;
        i++;
        j++;
        }
        if(b[j]=='\0'&& a[j]=='\0')
        break;
    else if(b[j]=='\0'){

   j=0;
   
   i=k; 
    count++;
}

    }
}printf("\n%d",count);
}
